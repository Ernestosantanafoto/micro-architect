# PDFs de documentación

Cada PDF es la versión en PDF del archivo `.md` homónimo en `docs/`.

**Regenerar:** desde la raíz del proyecto ejecuta `npm run docs:pdf` (requiere Node.js y `npm install`).
